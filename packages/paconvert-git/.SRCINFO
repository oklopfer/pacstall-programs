pkgname="paconvert-git"
gives="paconvert"
pkgver="0.0.1"
pkgdesc="Paconvert makes converting PKGBUILDS into pacscripts easier."
declare -a source=([0]="https://github.com/pacstall/paconvert.git")
declare -a breaks=([0]="paconvert-deb" [1]="paconvert-bin" [2]="paconvert-app" [3]="paconvert-git")
