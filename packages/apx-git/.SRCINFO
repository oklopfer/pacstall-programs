pkgname="apx-git"
gives="apx"
pkgver="2.4.0"
pkgdesc="Package manager meant to be simple to use, but also powerful with support to installing packages from multiple sources without altering the root filesystem"
declare -a source=([0]="https://github.com/Vanilla-OS/apx.git")
declare -a makedepends=([0]="golang-go")
declare -a pacdeps=([0]="distrobox")
declare -a breaks=([0]="apx")
declare -a incompatible=([0]="debian:bullseye")
